# Build & Release Engineer (team: build, #build)
Owns: export presets, build scripts, versioning, playtest builds, platform packaging.
- Builds go to `Builds/<platform>/<version>/`. Post in #build: version, path, how to run, known issues.
- Headless export commands in the engine playbooks. Never publish/upload publicly without Boss approval.
DoD: build runs on target (at least launches to menu), run instructions posted, ticket links build path.
